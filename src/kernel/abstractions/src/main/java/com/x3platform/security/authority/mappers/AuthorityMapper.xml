<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.x3platform.security.authority.mappers.AuthorityMapper">
  <resultMap id="BaseResultMap" type="com.x3platform.security.authority.Authority">
    <result property="id" jdbcType="VARCHAR" column="id" />
    <result property="name" jdbcType="VARCHAR" column="name" />
    <result property="description" jdbcType="VARCHAR" column="description" />
    <result property="locking" jdbcType="INTEGER" column="locking" />
    <result property="tags" jdbcType="VARCHAR" column="tags" />
    <result property="orderId" jdbcType="VARCHAR" column="order_id" />
    <result property="modifiedDate" jdbcType="TIMESTAMP" column="modified_date" />
    <result property="createdDate" jdbcType="TIMESTAMP" column="created_date" />
  </resultMap>
  <sql id="Select_Where_Clause">
    <where>
      <if test="param_id != null and param_id != ''">
        AND t.id = #{param_id}
      </if>
      <if test="param_name != null and param_name != ''">
        AND t.name = #{param_name}
      </if>
      <if test="param_description != null and param_description != ''">
        AND t.description = #{param_description}
      </if>
      <if test="param_locking != null">
        AND t.locking = #{param_locking}
      </if>
      <if test="param_tags != null and param_tags != ''">
        AND t.tags = #{param_tags}
      </if>
      <if test="param_order_id != null and param_order_id != ''">
        AND t.order_id = #{param_order_id}
      </if>
      <if test="param_modified_date != null">
        AND t.modified_date = #{param_modified_date}
      </if>
      <if test="param_created_date != null">
        AND t.created_date = #{param_created_date}
      </if>
    </where>
  </sql>
  <insert id="insert" parameterType="com.x3platform.security.authority.Authority">
    INSERT INTO sys_authority
    (
      id,
      name,
      description,
      locking,
      tags,
      order_id,
      modified_date,
      created_date
    )
    VALUES
    (
      #{id,jdbcType=VARCHAR},
      #{name,jdbcType=VARCHAR},
      #{description,jdbcType=VARCHAR},
      #{locking,jdbcType=INTEGER},
      #{tags,jdbcType=VARCHAR},
      #{orderId,jdbcType=VARCHAR},
      CURRENT_TIMESTAMP,
      CURRENT_TIMESTAMP
    )
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.x3platform.security.authority.Authority">
    UPDATE sys_authority SET

      name = #{name,jdbcType=VARCHAR},
      description = #{description,jdbcType=VARCHAR},
      locking = #{locking,jdbcType=INTEGER},
      tags = #{tags,jdbcType=VARCHAR},
      order_id = #{orderId,jdbcType=VARCHAR},
      modified_date = CURRENT_TIMESTAMP

    WHERE id = #{id}
  </update>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    DELETE FROM sys_authority WHERE id = #{id}
  </delete>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap" >
    SELECT * FROM sys_authority WHERE id = #{id}
  </select>
  <select id="findOneByName" parameterType="java.lang.String" resultMap="BaseResultMap" >
    SELECT * FROM sys_authority WHERE name = #{name}
  </select>
  <select id="findAll" parameterType="java.util.Map" resultMap="BaseResultMap" >
    SELECT * FROM sys_authority t
    <if test="_parameter != null">
      <include refid="Select_Where_Clause" />
    </if>
    <if test="orders != null and orders.size() > 0">
      ORDER BY
      <foreach collection="orders" item="order" index="index" separator="," >
        ${order}
      </foreach>
    </if>
    <if test="length > 0">
      LIMIT 0, ${length}
    </if>
  </select>
  <select id="getRowCount" >
    SELECT COUNT(0) AS "RowCount" FROM sys_authority t
    <if test="_parameter != null">
      <include refid="Select_Where_Clause" />
    </if>
  </select>
  <select id="isExist" resultType="java.lang.Boolean" >
    SELECT COUNT(0) AS "Count" FROM sys_authority WHERE id = #{id}
  </select>
</mapper>
